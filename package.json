{
  "name": "yaml-go-to-definition",
  "displayName": "yaml-go-to-definition",
  "description": "This is simple extension that allows you to go to the definition of a whatever in a yaml file.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:yaml"
  ],
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/glob": "^8.1.0",
    "@types/node": "20.2.5",
    "glob": "^8.1.0",
    "typescript": "^5.1.3"
  }
}
